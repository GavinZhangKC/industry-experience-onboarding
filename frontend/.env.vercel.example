# Add this in the Vercel frontend project's Environment Variables page.
# Replace it with the production backend URL, without a trailing slash.
VITE_API_BASE_URL=https://your-backend-project.vercel.app
